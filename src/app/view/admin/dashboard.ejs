<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Dash board</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" href="assets/images/decoration/icon-home.png">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
          rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
          crossorigin="anonymous">
        <link rel="stylesheet" href="/css/dashboard.css">
        <link rel="stylesheet" href="/css/productHeader.css">
        <% if(page === 'supperAdmin'){ %>

        <% } else if(page === 'home'){%>

        <% } else if(page === 'product'){%>

        <% } else if(page === 'category'){%>

        <% } else if(page === 'sale'){%>

        <% } else if(page === 'order'){%>

        <% } %>
</head>
<body>
<div class="container-fluid" style="height: 100%">
    <div class="row h-100">
        <div class="col-2" id="block_dashboard" style="position: fixed">
                <div id="header_dashboard">
                        <div id="title_dashboard">ATN</div>
                    </div>
                    <div id="body_dashboard">
                        <% if(role !== 1) {%>
                        <div class="controller-in-dashboard" id="dashboard_controller" data-page="dashboard"><i class="bi-house"></i> Home </div>
                        <div class="controller-in-dashboard" id="product_controller" data-page="product"><i class="bi-slack"></i> Product </div>
                        <div class="controller-in-dashboard" id="category_controller" data-page="category"><i class="bi-tags"></i> Category </div>
                        <div class="controller-in-dashboard" id="sale_controller" data-page="sale"><i class="bi-shop"></i> Sales </div>
                        <div class="controller-in-dashboard" id="order_controller" data-page="order"><i class="bi-shop"></i> Order </div>
                        <div class="controller-in-dashboard" id="statistical_controller" data-page="statistical"><i class="bi-bar-chart-line"></i> Statistical </div>
                        <% } else {%>
                        <div class="controller-in-dashboard" id="supper_admin_controller" data-page="supper-admin"><i class="bi-speedometer2"></i> Supper admin </div>
                        <% } %>
                        <a href="/logout" style="text-decoration: none; color: black"><div class="controller-in-dashboard"><i class="bi-power"></i> Logout</div></a>
                    </div>
        </div>
        <div class="col" style="margin-left: 17rem;">
            <div id="root_body">
                <% if(page === 'supperAdmin'){ %>
                    <%- include('./product/supperAdminIndex.ejs') %>
                <% } else if(page === 'home'){%>
                    <%- include('./product/home.ejs') %>
                <% } else if(page === 'product'){%>
                    <%- include('./product/productIndex.ejs') %>
                <% } else if(page === 'category'){%>
                    <%- include('./product/categoryIndex.ejs') %>
                <% } else if(page === 'sale'){%>
                    <%- include('./product/saleIndex.ejs') %>
                <% } else if(page === 'order'){%>
                    <%- include('./product/categoryIndex.ejs') %>
                <% } %>
            </div>
        </div>
    </div>
</div>
</body>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js"
            integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
    <script src="/js/dashboard/dashboard.js"></script>
    <% if(page === 'supperAdmin'){ %>

    <% } else if(page === 'home'){%>

    <% } else if(page === 'product'){%>

    <% } else if(page === 'category'){%>

    <% } else if(page === 'sale'){%>

    <% } else if(page === 'order'){%>

    <% } %>
</html>
